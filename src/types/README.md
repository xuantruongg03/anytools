# Types

TypeScript type definitions and interfaces.

## Guidelines

- Define shared types and interfaces here
- Use `.types.ts` suffix for type-only files
- Organize by domain (e.g., `user.types.ts`, `product.types.ts`)
